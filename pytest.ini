[pytest]
testpaths = autotests
addopts = -ra
markers =
    authenticated: requires credentials of a test student or administrator
    admin: requires administrator credentials
    student: requires student credentials
    mutation: changes data or consumes an attempt; disabled unless explicitly enabled
    production: intended to run against the production URL
